Run your own cases from here by creating a new directory and setting up the
simulation in a `run.py` script. See the verification cases for examples.

Note, everything in this directory is git-ignored.